# Phase 4 后续非阻断整理项

以下项目不应与当前 P0 启用阻断混在一起：

- 让安全扫描器默认排除 `.venv`，减少第三方库 Authorization 字样的误报。
- 候选 run manifest 改用相对路径，避免部署目录绝对路径提示。
- 为非 Git 工作区增加独立源码版本标识，便于后续差异审计。
- 统一 Windows 控制台 UTF-8 输出，避免终端中文显示乱码；UTF-8 日志与 JSON 当前不受影响。
- 正式启用后补做 24 小时运行观察和首次定时任务回顾。
